Category chip — used for filters and selected interests. More interactive than Badge.

```jsx
<Tag onClick={toggle} selected={active}>Fitness</Tag>
<Tag onRemove={() => remove(id)}>Instagram</Tag>
```
