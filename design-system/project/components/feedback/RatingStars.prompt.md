Star rating — a key trust signal. Read-only (supports half stars) or interactive input.

```jsx
<RatingStars value={4.9} count={128} showValue />
<RatingStars value={4.5} size={14} />
<RatingStars value={0} interactive onChange={(v) => setRating(v)} />
```
