Trust signal central to SponsorMatch — verified identity, Pro account, secure payment. Self-contained (no Icon dependency).

```jsx
<VerifiedBadge type="verified" />
<VerifiedBadge type="secure" />
<VerifiedBadge type="pro" size="sm" />
<VerifiedBadge type="verified" showLabel={false} />  {/* icon chip beside a name */}
```
