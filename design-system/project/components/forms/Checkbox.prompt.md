Checkbox with optional label and description line. Fills teal (`--accent`) when checked.

```jsx
<Checkbox label="AGB akzeptieren" />
<Checkbox label="Newsletter" description="Tipps für bessere Deals, max. 1× pro Monat" defaultChecked />
```
